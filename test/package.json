{
  "name": "jest-allure-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "jest": "^24.1.0",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "allure-results": "ALLURE_RESULT_FOLDER=build/allure-results jest --config jest-allure.config.js sample",
    "test": "jest src",
    "verify": "run-s allure-results test"
  }
}
